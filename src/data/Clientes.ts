type Clientes = {
    id: number;
    name: string;
    phone: number;
    senha: string;
    image: number;
    recurrence: boolean;
    recurrence_day: string;
    recurrence_hour: string;
    recurrence_mode: string;
};

export default Clientes